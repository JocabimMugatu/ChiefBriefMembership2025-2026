import './style.css'

const app = document.querySelector('#app')

if (app) {
  app.innerHTML = `
    <main>
      <h1>Netlify-ready Vite Application</h1>
      <p>Edit <code>src/main.js</code> to start building your app.</p>
    </main>
  `
}
